const animation = {
  initial: {
    opacity: 0,
    transition: `transform 0.15s ease-in-out`,
  },
  animate: {
    opacity: 1,
    transition: `transform 0.15s ease-in-out`,
  },
  exit: {
    opacity: 0,
    transition: `transform 0.15s ease-in-out`,
  },
};

export default animation;
